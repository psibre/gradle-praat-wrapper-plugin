processResources {
    from zipTree("$copyPraatDependency.destinationDir/$praatName-$praatVersion-${praatClassifier}.$praatExtension"), {
        filesMatching 'praatcon.exe', {
            it.path = 'org/praat/praat'
        }
    }
}
